defmodule TypeTalkTest do
  use ExUnit.Case
  import TypeTalkTestHelper

  test "get direct messages of account name" do
    # auth = access_token()
    # {:ok, res} = TypeTalk.messages_of_account(auth, "shimokawa")
    # assert res["directMessage"] != nil
  end
end
